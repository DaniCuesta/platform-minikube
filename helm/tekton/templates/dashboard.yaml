apiVersion: v1
kind: Service
metadata:
  name: tekton-dashboard
  namespace: tekton-pipelines
spec:
  type: ClusterIP
  ports:
    - port: 9097
      targetPort: 9097
  selector:
    app.kubernetes.io/part-of: tekton-dashboard
