apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: example-task
spec:
  steps:
    - name: example-step
      image: docker.io/alpine:3.14
      command:
        - echo
      args:
        - "Hello, Tekton!"
